server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
    
    location /api/fact {
        proxy_pass http://facts:8081/;
        proxy_set_header Host $host;
    }
}
